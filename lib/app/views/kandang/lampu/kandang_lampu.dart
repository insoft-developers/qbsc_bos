import 'package:flutter/material.dart';

class KandangLampu extends StatelessWidget {
  const KandangLampu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
