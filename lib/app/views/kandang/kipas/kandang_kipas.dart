import 'package:flutter/material.dart';

class KandangKipas extends StatelessWidget {
  const KandangKipas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
